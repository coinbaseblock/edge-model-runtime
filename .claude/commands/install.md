---
description: First-time install (creates .env, pulls core images, starts core stack)
---

Run the installer:

```bash
bash scripts/00-install.sh
```

After it finishes, suggest pulling a small model with `/pull qwen2.5-coder:7b`
or a Nemotron variant with `/nemotron`.
